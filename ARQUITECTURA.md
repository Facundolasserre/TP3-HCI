# Arquitectura del Backend - BagIt

## Diagrama de Capas

```
┌────────────────────────────────────────────────────────────────┐
│                         UI LAYER (Compose)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  Login   │  │  Lists   │  │ Products │  │ Pantries │       │
│  │  Screen  │  │  Screen  │  │  Screen  │  │  Screen  │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
└───────┼─────────────┼─────────────┼─────────────┼──────────────┘
        │             │             │             │
        │ hiltViewModel()           │             │
        │             │             │             │
┌───────▼─────────────▼─────────────▼─────────────▼──────────────┐
│                    VIEWMODEL LAYER (MVVM)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │    Auth      │  │  Shopping    │  │   Product    │         │
│  │  ViewModel   │  │     List     │  │  ViewModel   │         │
│  │              │  │  ViewModel   │  │              │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Pantry     │  │   Purchase   │  │   Category   │         │
│  │  ViewModel   │  │  ViewModel   │  │  (in Product)│         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
└─────────┼──────────────────┼──────────────────┼────────────────┘
          │                  │                  │
          │ @Inject          │                  │
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼────────────────┐
│                   REPOSITORY LAYER (Data)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │    User      │  │  Shopping    │  │   Product    │         │
│  │  Repository  │  │     List     │  │  Repository  │         │
│  │              │  │  Repository  │  │              │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Pantry     │  │   Purchase   │  │   Category   │         │
│  │  Repository  │  │  Repository  │  │  Repository  │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                 │
│         │  ┌──────────────────────────┐      │                 │
│         └──┤    ListItem Repository   ├──────┘                 │
│            └──────────┬───────────────┘                        │
│                       │  ┌────────────────────────┐            │
│                       └──┤ PantryItem Repository  │            │
│                          └──────────┬─────────────┘            │
└─────────────────────────────────────┼────────────────────────┘
                                      │
                                      │ Retrofit
                                      │
┌─────────────────────────────────────▼────────────────────────┐
│                     API SERVICES (Retrofit)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │    User      │  │  Shopping    │  │   Product    │       │
│  │ ApiService   │  │     List     │  │ ApiService   │       │
│  │              │  │  ApiService  │  │              │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                  │                  │               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   Pantry     │  │   Purchase   │  │   Category   │       │
│  │ ApiService   │  │ ApiService   │  │ ApiService   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                  │                  │               │
│         │  ┌──────────────────────────┐      │               │
│         └──┤  ListItem  ApiService    ├──────┘               │
│            └──────────┬───────────────┘                      │
│                       │  ┌────────────────────────┐          │
│                       └──┤ PantryItem ApiService  │          │
│                          └──────────┬─────────────┘          │
└─────────────────────────────────────┼──────────────────────┘
                                      │
                                      │ HTTP
                                      │
┌─────────────────────────────────────▼──────────────────────────┐
│                    NETWORK LAYER (OkHttp)                       │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              OkHttpClient                               │   │
│  │  ┌──────────────────┐  ┌────────────────────────┐     │   │
│  │  │ Auth Interceptor │  │ Logging Interceptor     │     │   │
│  │  │ (JWT Token)      │  │ (HTTP Logs)             │     │   │
│  │  └──────────────────┘  └────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
                                      │
                                      │
┌─────────────────────────────────────▼──────────────────────────┐
│                    DEPENDENCY INJECTION (Hilt)                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                   NetworkModule                         │   │
│  │  • Retrofit                                             │   │
│  │  • OkHttpClient                                         │   │
│  │  • All ApiServices                                      │   │
│  │  • DataStore (Token Storage)                            │   │
│  └────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
                                      │
                                      │ REST API
                                      │
┌─────────────────────────────────────▼──────────────────────────┐
│                    EXTERNAL API SERVER                          │
│              http://localhost:8080/api/                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Users   │  │  Lists   │  │ Products │  │ Pantries │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
│  ┌──────────┐  ┌──────────┐                                   │
│  │Categories│  │Purchases │                                   │
│  └──────────┘  └──────────┘                                   │
└────────────────────────────────────────────────────────────────┘
```

## Flujo de Datos

### 1. Autenticación (Login)
```
LoginScreen
    └─> AuthViewModel.login(email, password)
        └─> UserRepository.login(LoginRequest)
            └─> UserApiService.login(credentials)
                └─> POST /api/users/login
                    └─> Response: { token: "..." }
                        └─> Save to DataStore
                            └─> Update UI State
```

### 2. Obtener Listas de Compras
```
ShoppingListScreen
    └─> ShoppingListViewModel.getShoppingLists()
        └─> ShoppingListRepository.getShoppingLists(params)
            └─> ShoppingListApiService.getShoppingLists(...)
                └─> GET /api/shopping-lists?page=1&per_page=10
                    └─> Auth Interceptor adds: Authorization: Bearer <token>
                        └─> Response: PaginatedResponse<ShoppingList>
                            └─> Emit Result.Success(data)
                                └─> Update listsState
                                    └─> UI recomposes with data
```

### 3. Crear Producto
```
ProductScreen
    └─> ProductViewModel.createProduct(name, categoryId)
        └─> ProductRepository.createProduct(ProductRequest)
            └─> ProductApiService.createProduct(request)
                └─> POST /api/products
                    └─> Body: { name: "Milk", category: { id: 1 } }
                        └─> Response: Product
                            └─> Emit Result.Success(product)
                                └─> Auto-refresh products list
```

## Modelos de Datos (DTOs)

```
User ─────────┐
              ├──> ShoppingList
Product ──────┤        │
              │        ├──> ListItem ──> Product ──> Category
Category ─────┘        │
                       └──> Owner: User
                       └──> SharedWith: List<User>

Pantry ────────┐
               ├──> PantryItem ──> Product ──> Category
Product ───────┘

Purchase
    └──> ShoppingList
    └──> List<ListItem>
    └──> Owner: User
```

## Endpoints por Módulo

### 👤 Users (11 endpoints)
- POST `/api/users/register`
- POST `/api/users/login`
- POST `/api/users/logout`
- GET `/api/users/profile`
- PUT `/api/users/profile`
- POST `/api/users/verify-account`
- POST `/api/users/send-verification`
- POST `/api/users/forgot-password`
- POST `/api/users/reset-password`
- POST `/api/users/change-password`

### 📦 Categories (5 endpoints)
- POST `/api/categories`
- GET `/api/categories`
- GET `/api/categories/{id}`
- PUT `/api/categories/{id}`
- DELETE `/api/categories/{id}`

### 🏷️ Products (5 endpoints)
- POST `/api/products`
- GET `/api/products`
- GET `/api/products/{id}`
- PUT `/api/products/{id}`
- DELETE `/api/products/{id}`

### 🛒 Shopping Lists (11 endpoints)
- POST `/api/shopping-lists`
- GET `/api/shopping-lists`
- GET `/api/shopping-lists/{id}`
- PUT `/api/shopping-lists/{id}`
- DELETE `/api/shopping-lists/{id}`
- POST `/api/shopping-lists/{id}/purchase`
- POST `/api/shopping-lists/{id}/reset`
- POST `/api/shopping-lists/{id}/move-to-pantry`
- POST `/api/shopping-lists/{id}/share`
- GET `/api/shopping-lists/{id}/shared-users`
- DELETE `/api/shopping-lists/{id}/share/{user_id}`

### ✏️ List Items (5 endpoints)
- POST `/api/shopping-lists/{id}/items`
- GET `/api/shopping-lists/{id}/items`
- PUT `/api/shopping-lists/{id}/items/{item_id}`
- PATCH `/api/shopping-lists/{id}/items/{item_id}`
- DELETE `/api/shopping-lists/{id}/items/{item_id}`

### 🏺 Pantries (8 endpoints)
- POST `/api/pantries`
- GET `/api/pantries`
- GET `/api/pantries/{id}`
- PUT `/api/pantries/{id}`
- DELETE `/api/pantries/{id}`
- POST `/api/pantries/{id}/share`
- GET `/api/pantries/{id}/shared-users`
- DELETE `/api/pantries/{id}/share/{user_id}`

### 📋 Pantry Items (4 endpoints)
- POST `/api/pantries/{id}/items`
- GET `/api/pantries/{id}/items`
- PUT `/api/pantries/{id}/items/{item_id}`
- DELETE `/api/pantries/{id}/items/{item_id}`

### 🧾 Purchases (3 endpoints)
- GET `/api/purchases`
- GET `/api/purchases/{id}`
- POST `/api/purchases/{id}/restore`

## Total: 52 endpoints implementados ✅

